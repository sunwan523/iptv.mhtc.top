services:
  iptv-local:
    image: ghcr.nju.edu.cn/sunwan523/iptv-local:latest
    container_name: iptv-local
    restart: always
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8787
      - DATA_DIR=/app/data
